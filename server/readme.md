To generate admin and accountant credentials use npm run scripts

Add env variables first : 

ADMIN_EMAIL=""
ACCOUNTANT_EMAIL=""

Then `npm run create:admin` for admin and `create:accountant` for accountant